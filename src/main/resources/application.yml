spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
minio:
  # OSS地址
  endpoint: http://192.168.216.128:9000
  # 密钥
  access-key: minio
  secret-key: minio123
server:
  port: 8081