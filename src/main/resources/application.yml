spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
minio:
  # OSS地址
  endpoint: http://192.168.216.128:9000
  # 默认存储桶
  bucketName: lishunyi
  # 密钥
  accessKey: minio
  secretKey: minio123
